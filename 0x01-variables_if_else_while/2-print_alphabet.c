#include <stdio.h>

/**
   * main - Prints the alphabet in lowercase.
    *
     * Return: Always 0.
      */
int main(void)
{
	char xy;

	for (xy = 'a'; xy <= 'z'; xy++)
		putchar(xy);

	putchar('\n');

	return (0);
}
