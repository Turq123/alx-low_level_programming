#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - printing all single digits of base 10
 *
 * Return: Always 0.
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d \n", num);

	return (0);
}
